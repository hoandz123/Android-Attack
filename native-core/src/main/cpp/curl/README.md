# cURL (vendored)

Prebuilt from [vvb2060/ndk curl 8.18.0](https://repo1.maven.org/maven2/io/github/vvb2060/ndk/curl/8.18.0/) + boringssl 20251124 (one-time extract).

Per ABI: `libcurl.a`, `libssl.a`, `libcrypto.a`, `libnghttp2.a`, `libnghttp3.a`, `libngtcp2.a`, `libngtcp2_crypto.a`.

Headers: `include/curl/`. No Gradle/Maven dependency at build time.
